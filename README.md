# CS748-distributed-join

- Project (That's where our main codes lie)
  - trial (Our test codes for reading a csv)
  - dataset (Our script for creating dataset and also generated datasets)
    - pointSetGeneration.py (Generate one dimension point dataset for similarity join) eg: python pointSetGeneration.py 1000 pointDataSet/pointDataSet1.csv
- prev (Reference codes)
